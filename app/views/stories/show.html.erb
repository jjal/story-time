<% provide(:title, @story.title) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @story.title %>
      </h1>
    </section>
    <%= @story.description %>
  </aside>
</div>

<%= link_to 'Edit', edit_story_path(@story) %> |
<%= link_to 'Back', stories_path %>
