<% provide(:title, "New Story") %>
<h1>New Story</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@story) do |f| %>
      <%= render "fields", f: f %>
      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>