<%= will_paginate @stories %>
<ul class="thumbnails">
  <%= render @stories %>
</ul>
<%= will_paginate @stories %>
<% if(signed_in?) %>
  <%= button_to "Add a story", new_story_path, method: :get, class: "btn btn-large btn-primary" %>
<% end %>