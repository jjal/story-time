<% provide(:title, @page.title) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @page.title %>
      </h1>
    </section>
    <%= @page.description %>
  </aside>
</div>

<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>