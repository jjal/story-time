<% provide(:title, @page.title) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @page.title %>
      </h1>
    </section>
    <section>
    <%= @page.text %>
    </section>
    <section class="link_section">
    <ul>
    <%= render @page.links %>
    </ul>
    </section>
  </aside>
</div>

<%= link_to "Edit", edit_story_page_path(@story,@page) %> |
<%= link_to "Back", story_pages_path(@story) %>